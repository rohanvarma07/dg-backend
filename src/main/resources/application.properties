spring.application.name=dg-backend

# Server Configuration - Railway will provide PORT via environment variable
server.port=${PORT:8080}

# Upload directory configuration - adjust for Railway deployment
app.upload.dir=./uploads

# File upload size limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Database configuration - Railway MySQL
spring.datasource.url=jdbc:mysql://root:IiBHetIaeOWezDfXHoDDPCtXsilVHPwK@ballast.proxy.rlwy.net:53200/railway
spring.datasource.username=root
spring.datasource.password=IiBHetIaeOWezDfXHoDDPCtXsilVHPwK
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Data initialization settings
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true
spring.sql.init.continue-on-error=false

# Connection pool settings
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=12
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# Enhanced Logging for debugging
